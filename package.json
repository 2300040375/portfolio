{
  "name": "portfolio-smtp-contact",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio contact form with SMTP email sending via a small Node.js backend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nodemailer": "^6.9.14"
  }
}
